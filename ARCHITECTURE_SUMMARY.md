# 🏗️ Arquitectura del Sistema

## 📊 Diagrama de Arquitectura

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (Next.js)                       │
│                      http://localhost:3000                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Pages      │  │  Components  │  │   Contexts   │         │
│  ├──────────────┤  ├──────────────┤  ├──────────────┤         │
│  │ / (Home)     │  │ HomeHeader   │  │ AuthContext  │         │
│  │ /login       │  │ HeroSection  │  │              │         │
│  │ /register    │  │ CategoryGrid │  └──────────────┘         │
│  │ /search      │  │ Benefits     │                            │
│  │              │  │ Testimonials │  ┌──────────────┐         │
│  └──────────────┘  │ SearchBar    │  │   Services   │         │
│                    │ ResultsList  │  ├──────────────┤         │
│                    │ Protected    │  │ auth.service │         │
│                    │ GuestRoute   │  │ practitioners│         │
│                    └──────────────┘  │   .service   │         │
│                                      └──────────────┘         │
│                                                                  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ HTTP/REST + Cookies
                         │ (JWT in HTTP-only cookies)
                         │
┌────────────────────────▼────────────────────────────────────────┐
│                        BACKEND (NestJS)                          │
│                      http://localhost:3001                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │ Controllers  │  │   Services   │  │     DTOs     │         │
│  ├──────────────┤  ├──────────────┤  ├──────────────┤         │
│  │ AuthCtrl     │  │ AuthService  │  │ RegisterDTO  │         │
│  │ Practitioners│  │ Practitioners│  │ LoginDTO     │         │
│  │   Ctrl       │  │   Service    │  │ SearchDTO    │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Guards     │  │  Strategies  │  │  Decorators  │         │
│  ├──────────────┤  ├──────────────┤  ├──────────────┤         │
│  │ JwtAuthGuard │  │ JwtStrategy  │  │ @Public()    │         │
│  │              │  │              │  │ @CurrentUser │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ Prisma ORM
                         │
┌────────────────────────▼────────────────────────────────────────┐
│                      PostgreSQL Database                         │
│                      localhost:5432                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────┐  ┌──────────────┐  ┌─────────┐  ┌──────────┐   │
│  │  Users   │  │ Professionals│  │ Services│  │ Reviews  │   │
│  ├──────────┤  ├──────────────┤  ├─────────┤  ├──────────┤   │
│  │ id       │  │ id           │  │ id      │  │ id       │   │
│  │ email    │  │ slug         │  │ title   │  │ rating   │   │
│  │ password │  │ name         │  │ price   │  │ comment  │   │
│  │ name     │  │ city         │  │ ...     │  │ ...      │   │
│  │ role     │  │ verified     │  └─────────┘  └──────────┘   │
│  │ ...      │  │ rating       │                                │
│  └──────────┘  │ ...          │  ┌──────────┐                 │
│                └──────────────┘  │  Leads   │                 │
│                                  ├──────────┤                 │
│                                  │ id       │                 │
│                                  │ userName │                 │
│                                  │ message  │                 │
│                                  │ status   │                 │
│                                  └──────────┘                 │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 🔐 Flujo de Autenticación

```
┌─────────┐                                    ┌─────────┐
│ Client  │                                    │ Server  │
└────┬────┘                                    └────┬────┘
     │                                              │
     │  1. POST /auth/register                     │
     │  { email, password, name }                  │
     ├────────────────────────────────────────────►│
     │                                              │
     │                                              │ 2. Hash password
     │                                              │    Create user
     │                                              │    Generate JWT
     │                                              │
     │  3. Set-Cookie: accessToken (HTTP-only)     │
     │     { user, accessToken }                   │
     │◄────────────────────────────────────────────┤
     │                                              │
     │  4. Store user in AuthContext               │
     │     Redirect to home                        │
     │                                              │
     │  5. GET /practitioners                      │
     │     Cookie: accessToken                     │
     ├────────────────────────────────────────────►│
     │                                              │
     │                                              │ 6. Verify JWT
     │                                              │    Query DB
     │                                              │
     │  7. { data: [...], meta: {...} }            │
     │◄────────────────────────────────────────────┤
     │                                              │
     │  8. POST /auth/logout                       │
     │     Cookie: accessToken                     │
     ├────────────────────────────────────────────►│
     │                                              │
     │                                              │ 9. Clear cookies
     │                                              │
     │  10. Set-Cookie: accessToken=; Max-Age=0    │
     │◄────────────────────────────────────────────┤
     │                                              │
     │  11. Clear AuthContext                      │
     │      Redirect to home                       │
     │                                              │
```

## 📦 Estructura de Datos

### User
```typescript
{
  id: string (UUID)
  email: string (unique)
  password: string (hashed)
  name: string
  phone?: string
  role: 'USER' | 'PROFESSIONAL' | 'ADMIN'
  createdAt: DateTime
  updatedAt: DateTime
}
```

### Professional
```typescript
{
  id: string (UUID)
  slug: string (unique)
  name: string
  bio?: string
  city?: string
  postalCode?: string
  phone?: string
  email?: string (unique)
  licenseNumber?: string (unique)
  verified: boolean
  latitude?: number
  longitude?: number
  rating: number
  reviewCount: number
  services: Service[]
  reviews: Review[]
  createdAt: DateTime
  updatedAt: DateTime
}
```

### Service
```typescript
{
  id: string (UUID)
  professionalId: string
  title: string
  description?: string
  price?: number
  durationMinutes?: number
  createdAt: DateTime
  updatedAt: DateTime
}
```

### Review
```typescript
{
  id: string (UUID)
  professionalId: string
  userId?: string
  rating: number (1-5)
  comment?: string
  createdAt: DateTime
  updatedAt: DateTime
}
```

## 🔄 Flujo de Búsqueda

```
┌─────────────┐
│ User types  │
│ in search   │
└──────┬──────┘
       │
       ▼
┌─────────────────────┐
│ SearchBar component │
│ updates state       │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────────────┐
│ useEffect triggers search   │
│ with debounce (optional)    │
└──────┬──────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ practitionersService.search()│
│ with filters                 │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ API: GET /practitioners      │
│ ?q=...&city=...&verified=... │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ PractitionersService (NestJS)│
│ builds Prisma query          │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ Prisma queries PostgreSQL    │
│ with filters and pagination  │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ Return { data, meta }        │
│ with results and pagination  │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────┐
│ ResultsList component        │
│ displays results             │
└──────────────────────────────┘
```

## 🛡️ Seguridad

### Frontend
- ✅ Tokens JWT en cookies HTTP-only (no accesibles desde JavaScript)
- ✅ Validación de formularios en cliente
- ✅ Protección de rutas con HOCs
- ✅ HTTPS en producción (recomendado)

### Backend
- ✅ Passwords hasheados con bcrypt (10 rounds)
- ✅ JWT con expiración configurable
- ✅ Cookies con flags Secure y SameSite
- ✅ CORS configurado para dominio específico
- ✅ Validación de DTOs con class-validator
- ✅ Guards globales para proteger rutas
- ✅ Decorador @Public() para rutas públicas

## 🚀 Tecnologías

### Frontend
- **Next.js 16** - React framework con App Router
- **React 19** - UI library
- **TypeScript 5** - Type safety
- **Tailwind CSS 4** - Utility-first CSS
- **Context API** - State management

### Backend
- **NestJS 11** - Node.js framework
- **Prisma 6** - ORM
- **PostgreSQL** - Database
- **Passport.js** - Authentication
- **JWT** - Token-based auth
- **bcryptjs** - Password hashing
- **class-validator** - DTO validation

## 📈 Escalabilidad

### Horizontal
- Frontend: Deploy en Vercel/Netlify con CDN
- Backend: Deploy en múltiples instancias con load balancer
- Database: PostgreSQL con read replicas

### Vertical
- Caching con Redis
- Full-text search con Elasticsearch
- File storage con S3
- Background jobs con Bull/BullMQ

## 🔍 Monitoreo

### Recomendado
- **Sentry** - Error tracking
- **LogRocket** - Session replay
- **New Relic** - APM
- **Datadog** - Infrastructure monitoring

---

Esta arquitectura proporciona una base sólida para escalar y mantener la aplicación. 🎯

